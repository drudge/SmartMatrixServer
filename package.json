{
  "name": "smart-matrix-server",
  "version": "0.3.0",
  "description": "SmartMatrix Server",
  "main": "index.js",
  "scripts": {
    "start": "env $(cat .env | grep -v '#' | xargs) node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0",
    "mqtt": "^4.3.7",
    "ms": "^2.1.3",
    "node-fetch-commonjs": "^3.2.4",
    "toad-scheduler": "^2.2.0",
    "watchdog": "^0.8.17"
  }
}
